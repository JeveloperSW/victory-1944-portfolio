# 클라이언트 빌드 설정 (D-039). 이 파일을 .env.local로 복사해 쓴다.
#
# API 주소. 값이 없으면 http://127.0.0.1:8150으로 떨어진다.
# 배포 빌드는 반드시 HTTPS 주소여야 한다 — 평문 HTTP는 설정 화면에 경고가 뜨고,
# Android는 기본 정책상 평문 트래픽을 차단한다.
VITE_API_BASE_URL=http://127.0.0.1:8150
